"use client";

import { useEffect, useState } from "react";
import Image from "next/image";
import Link from "next/link";

const slides = [
  {
    image: "/banners/hero-01.png",
    headline: "Elevate Your Everyday",
    subheading: "Timeless essentials for modern living.",
    button: "SHOP NOW",
    href: "/collections/new",
  },
  {
    image: "/banners/hero-02.png",
    headline: "Redefine Your Style",
    subheading: "Premium fashion designed to stand out.",
    button: "EXPLORE COLLECTION",
    href: "/collections/all",
  },
  {
    image: "/banners/hero-03.png",
    headline: "New Season. New You.",
    subheading: "Discover the latest arrivals.",
    button: "VIEW NEW ARRIVALS",
    href: "/collections/new",
  },
  {
    image: "/banners/hero-04.png",
    headline: "Crafted for Confidence",
    subheading: "Fashion that moves with you.",
    button: "SHOP MEN",
    href: "/collections/men",
  },
  {
    image: "/banners/hero-05.png",
    headline: "Wear the Difference",
    subheading: "Quality you can feel.",
    button: "SHOP NOW",
    href: "/collections/all",
  },
  {
    image: "/banners/hero-06.png",
    headline: "Limited Edition",
    subheading: "Exclusive pieces, limited stock.",
    button: "DISCOVER MORE",
    href: "/collections/new",
  },
  {
    image: "/banners/hero-07.png",
    headline: "Everyday Luxury",
    subheading: "Minimal. Elegant. Timeless.",
    button: "SHOP COLLECTION",
    href: "/collections/all",
  },
  {
    image: "/banners/hero-08.png",
    headline: "Bold Looks Start Here",
    subheading: "Make every outfit unforgettable.",
    button: "SHOP NOW",
    href: "/collections/all",
  },
  {
    image: "/banners/hero-09.png",
    headline: "Your Signature Style",
    subheading: "Curated collections for every occasion.",
    button: "EXPLORE COLLECTION",
    href: "/collections/all",
  },
  {
    image: "/banners/hero-10.png",
    headline: "Fresh Arrivals",
    subheading: "New trends, just dropped.",
    button: "VIEW ALL",
    href: "/collections/new",
  },
  {
    image: "/banners/hero-11.png",
    headline: "Premium Essentials",
    subheading: "Built for comfort and style.",
    button: "SHOP ESSENTIALS",
    href: "/collections/all",
  },
  {
    image: "/banners/hero-12.png",
    headline: "Style Without Compromise",
    subheading: "Modern fashion, exceptional quality.",
    button: "SHOP NOW",
    href: "/collections/all",
  },
  {
    image: "/banners/hero-13.png",
    headline: "Discover What's Next",
    subheading: "Explore our newest collection.",
    button: "EXPLORE NOW",
    href: "/collections/new",
  },
  {
    image: "/banners/hero-14.png",
    headline: "Fashion That Inspires",
    subheading: "Confidence begins with what you wear.",
    button: "SHOP NOW",
    href: "/collections/all",
  },
];

export default function HeroSlider() {
  const [current, setCurrent] = useState(0);

  useEffect(() => {
    const id = setInterval(
      () => setCurrent((c) => (c + 1) % slides.length),
      5000
    );
    return () => clearInterval(id);
  }, []);

  return (
    <section className="relative h-screen overflow-hidden">

      {slides.map((slide, index) => (
        <div
          key={slide.image}
          className={`absolute inset-0 transition-opacity duration-1000 ${
            current === index ? "opacity-100" : "opacity-0"
          }`}
        >
          <Image
            src={slide.image}
            alt={slide.headline}
            fill
            priority={index === 0}
            className="object-cover"
          />
          <div className="absolute inset-0 bg-black/30" />
        </div>
      ))}

      <div className="absolute inset-0 flex items-center">
        <div className="mx-auto w-full max-w-7xl px-6 text-white">

          <div className="mb-6 text-sm font-semibold tracking-[0.35em]">
            {(current + 1).toString().padStart(2,"0")} / 14
          </div>

          <h1 className="max-w-3xl text-5xl font-black leading-tight md:text-7xl">
            {slides[current].headline}
          </h1>

          <p className="mt-6 max-w-2xl text-xl text-white/90">
            {slides[current].subheading}
          </p>

          <Link
            href={slides[current].href}
            className="mt-10 inline-flex rounded-full bg-white px-8 py-4 font-semibold text-black transition hover:scale-105"
          >
            {slides[current].button}
          </Link>

        </div>
      </div>

      <button
        onClick={() => setCurrent((current - 1 + slides.length) % slides.length)}
        className="absolute left-6 top-1/2 -translate-y-1/2 rounded-full bg-white/20 p-3 text-white backdrop-blur"
      >
        ←
      </button>

      <button
        onClick={() => setCurrent((current + 1) % slides.length)}
        className="absolute right-6 top-1/2 -translate-y-1/2 rounded-full bg-white/20 p-3 text-white backdrop-blur"
      >
        →
      </button>

      <div className="absolute bottom-8 left-1/2 flex -translate-x-1/2 gap-3">
        {slides.map((_, i) => (
          <button
            key={i}
            onClick={() => setCurrent(i)}
            className={`h-2 rounded-full transition-all ${
              i === current ? "w-10 bg-white" : "w-2 bg-white/40"
            }`}
          />
        ))}
      </div>

    </section>
  );
}
